version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.prod
    volumes:
      - laragod_storage:/var/www/storage
    networks:
      - proxy-network
      - database-network
    depends_on:
      - mysql

volumes:
  laragod_storage:

networks:
  proxy-network:
    external: true
  database-network:
    driver: bridge
